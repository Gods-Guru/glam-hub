import React from 'react';
import './App.css';

const Hero = () => {
    return (
        <section className="hero">
            <div className="hero-content">
                <h1>Discover Beauty at GlamHub</h1>
                <p>Your go-to for all premium beauty products</p>
                <button className="shop-now-btn">Shop Now</button>
            </div>
        </section>
    );
};

export default Hero;
